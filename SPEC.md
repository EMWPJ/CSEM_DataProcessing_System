# CSEM数据处理系统 - 需求规格说明书

## 1. 引言

### 1.1 文档目的

本文档定义CSEM（可控源电磁法）数据处理系统的功能需求、性能需求和接口需求，作为系统开发的依据。

### 1.2 适用范围

本系统面向地球物理专业学生和研究人员，用于教学演示CSEM数据从原始时间序列到阻抗计算的全过程。

### 1.3 定义与缩写

| 缩写 | 全称 | 说明 |
|-----|------|-----|
| CSEM | Controlled-Source Electromagnetic | 可控源电磁法 |
| FFT | Fast Fourier Transform | 快速傅里叶变换 |
| PSD | Power Spectral Density | 功率谱密度 |
| CSD | Cross Spectral Density | 交叉谱密度 |
| GUI | Graphical User Interface | 图形用户界面 |

---

## 2. 系统概述

### 2.1 项目背景

CSEM是一种重要的地球物理勘探方法，需要专业的数据处理软件将采集的时间序列数据转换为电磁阻抗等地球物理参数。本系统作为教学演示平台，旨在帮助用户理解CSEM数据处理的完整流程。

### 2.2 系统边界

**输入**：
- 原始时间序列数据文件（二进制或CSV格式）
- 处理参数配置

**输出**：
- 处理后的时间序列数据
- 频谱分析结果
- 阻抗曲线数据
- 视电阻率曲线
- 相位曲线
- 可选：图片格式的处理结果图件

**不在范围内**：
- 野外数据采集
- 反演成像
- 报告自动生成

---

## 3. 功能需求

### 3.1 数据读取

| 需求编号 | 需求描述 | 优先级 | 备注 |
|---------|---------|--------|------|
| DR-001 | 支持读取CSV格式时间序列数据 | 必须 | 复用现有CSEMProcesss.py |
| DR-002 | 支持读取二进制原始数据格式 | 预留接口 | 具体格式待定 |
| DR-003 | 自动识别采样率 | 应该 | 从数据或配置获取 |
| DR-004 | 显示数据基本信息（长度、通道数、采样率） | 必须 | 在界面上显示 |

### 3.2 预处理

| 需求编号 | 需求描述 | 优先级 | 备注 |
|---------|---------|--------|------|
| PP-001 | 去除直流分量（去均值） | 必须 | 可开关 |
| PP-002 | 去除趋势（线性/多项式） | 应该 | 可选阶数 |
| PP-003 | 工频陷波滤波（50Hz/60Hz及谐波） | 必须 | 可开关 |
| PP-004 | 带通滤波（有效频段） | 应该 | 参数可调 |
| PP-005 | 显示预处理前后对比 | 必须 | 教学演示用 |

### 3.3 发射时段提取

| 需求编号 | 需求描述 | 优先级 | 备注 |
|---------|---------|--------|------|
| EE-001 | 发射时段提取接口定义 | 必须 | 具体逻辑待实现 |
| EE-002 | 支持多频率分别提取 | 应该 | 接口预留 |
| EE-003 | 发射时段可视化标记 | 应该 | 在时间序列上标记 |

### 3.4 频谱分析

| 需求编号 | 需求描述 | 优先级 | 备注 |
|---------|---------|--------|------|
| SA-001 | FFT频谱计算 | 必须 | |
| SA-002 | Welch功率谱密度估计 | 必须 | 支持分段平均 |
| SA-003 | 交叉谱计算 | 必须 | 用于相位 |
| SA-004 | 幅度谱和相位谱分离显示 | 必须 | |
| SA-005 | 多通道频谱叠加显示 | 应该 | 通道可选 |

### 3.5 阻抗计算

| 需求编号 | 需求描述 | 优先级 | 备注 |
|---------|---------|--------|------|
| IC-001 | 基于FFT的阻抗计算 Z = E/H | 必须 | |
| IC-002 | 张量阻抗计算 | 应该 | 2x2矩阵 |
| IC-003 | 阻抗幅度曲线显示 | 必须 | vs 频率 |
| IC-004 | 阻抗相位曲线显示 | 必须 | vs 频率 |
| IC-005 | 视电阻率曲线显示 | 必须 | ρa vs 频率 |

### 3.6 界面功能

| 需求编号 | 需求描述 | 优先级 | 备注 |
|---------|---------|--------|------|
| UI-001 | 文件打开对话框 | 必须 | |
| UI-002 | 参数配置面板 | 必须 | 采样率、滤波参数等 |
| UI-003 | 时间序列显示组件 | 必须 | 多通道 |
| UI-004 | 频谱显示组件 | 必须 | 幅度/相位 |
| UI-005 | 阻抗曲线显示组件 | 必须 | 多子图 |
| UI-006 | 步骤导航（前进/后退） | 必须 | 教学演示用 |
| UI-007 | 全流程一键处理 | 必须 | |
| UI-008 | 处理进度显示 | 应该 | |
| UI-009 | 结果导出功能 | 应该 | 图片/数据 |

---

## 4. 非功能需求

### 4.1 性能需求

| 指标 | 要求 | 说明 |
|-----|------|------|
| 启动时间 | < 5秒 | 从点击图标到界面显示 |
| 数据加载 | < 10秒 | 100MB以下文件 |
| 单步处理 | < 5秒 | 100万采样点 |
| 界面响应 | < 100ms | 不包括数据处理 |

### 4.2 可靠性需求

- 数据处理过程应有错误处理，不因异常数据崩溃
- 关键操作应有确认提示
- 应记录操作日志便于问题排查

### 4.3 可用性需求

- 界面布局清晰，操作流程直观
- 错误信息应明确指出问题原因
- 提供帮助提示或工具提示

### 4.4 兼容性需求

- 支持Windows 10/11操作系统
- 分辨率适应：1920x1080及以上

---

## 5. 用户界面需求

### 5.1 整体布局

- 单窗口多面板布局
- 左侧：控制面板（参数设置、步骤导航）
- 右侧：可视化区域（时间序列/频谱/阻抗）
- 底部：状态栏

### 5.2 颜色方案

- 默认使用系统主题
- 图表使用区分度高的颜色方案
- 支持高对比度模式（可选）

### 5.3 字体

- 界面：系统默认字体
- 图表： sans-serif 字体（如Arial, DejaVu Sans）

---

## 6. 数据格式需求

### 6.1 输入数据格式

**CSV格式**：
```
时间,Ex,Ey,Hx,Hy,Current
0.0,0.001,0.002,0.0001,0.0002,1.0
0.005,0.0012,0.0021,0.00011,0.00021,1.0
...
```

**二进制格式**（预留接口）：
- 文件头：采样率、通道数、时间戳等信息
- 数据体：连续采样的二进制数据

### 6.2 输出数据格式

**处理结果（内部）**：
- 使用Python数据结构在内存中传递

**导出格式**：
- 图片：PNG、SVG
- 数据：CSV

---

## 7. 约束条件

### 7.1 技术约束

- 使用Python 3.9+语言
- 使用PySide6作为GUI框架
- 使用NumPy、SciPy进行科学计算
- 使用Matplotlib进行可视化

### 7.2 项目约束

- 开发周期：4周
- 优先保证教学演示功能的完整性
- 具体二进制数据格式待用户提供

---

## 8. 验收标准

### 8.1 功能验收

| 测试项 | 验收条件 |
|-------|---------|
| 数据读取 | 能正确读取CSV文件并显示基本信息 |
| 预处理 | 滤波前后波形有可见差异 |
| 频谱分析 | 频谱图能显示主要频率成分 |
| 阻抗计算 | 能输出阻抗幅度和相位曲线 |
| 步骤导航 | 能前进后退到任意步骤 |
| 全流程处理 | 一键完成所有处理步骤 |

### 8.2 界面验收

| 测试项 | 验收条件 |
|-------|---------|
| 窗口显示 | 主窗口正常显示，无布局错乱 |
| 图表显示 | Matplotlib图表正确嵌入Qt窗口 |
| 响应性 | 操作无明显卡顿 |
| 中文显示 | 中文文字正确显示 |

---

## 9. 待确定事项

以下事项需要在后续阶段与用户确认：

1. **二进制数据格式**：具体是哪种仪器的数据？文件头结构如何？
2. **发射时段提取逻辑**：使用什么方法（相关检测、噪声门限等）？
3. **几何因子默认值**：根据实际测量布置确定
4. **输出报告格式**：是否需要生成正式报告？

---

*文档版本：v1.0*
*创建日期：2026-03-19*
